# Local Development URL below :
# (note: if using docker postgres , paste the connection string with proper port mapping and container name ( if not localhost. )

DATABASE_URL="postgresql://postgres:root@localhost:5431/urlshortener?schema=public"

# Want to test cloud database (i.e. NeonDB) , create neondb account paste connection URI under DATABASE_URL
